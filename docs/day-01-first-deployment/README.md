# Day 1 - First Deployment

## Goal

Get the project from zero to a deployable Next.js application. This day establishes the repo, the initial Next.js App Router project, and the deployment-oriented Docker/standalone foundation.

## Lectures Covered

- Lecture 6 - Create Next.js App (TypeScript + App Router)
- Lecture 7 - Create a Simple Page
- Lecture 8 - GitHub Repo Setup
- Lecture 9 - Deploy to DigitalOcean (First Release)
- Lecture 10 - Recap Day 1

## Commit Evidence

Commits found for this day:

- `2846932` - Add Readme.md
- `1cef65d` - Add tsconfig.json
- `c20f895` - Add .gitignore
- `7161301` - Add all boilerplate files
- `6c1b9eb` - Cahnge tilte in page.tsx
- `69a7f8f` - Add Dockerfile
- `e80545f` - Add next.js standalone

## Final State

By the end of the day, the project has:

- A Next.js app using the App Router.
- TypeScript configuration through `tsconfig.json`.
- Initial app files under `app/`.
- Global CSS setup under `app/globals.css`.
- Basic public assets generated by the Next.js starter.
- A `.gitignore`.
- A `Dockerfile`.
- `next.config.ts` configured with `output: "standalone"` for container deployment.

## Key Files

- `app/layout.tsx`
- `app/page.tsx`
- `app/globals.css`
- `package.json`
- `tsconfig.json`
- `next.config.ts`
- `Dockerfile`

## Teaching Narrative

The course starts with a production-first mindset: ship something immediately, even if it is simple. Students see the full loop from code to repository to deployment before any feature work begins.

The important teaching point is that deployment is not saved for the end. The application is designed from the beginning to run outside the local machine.

## Notes

- The typo in commit message `Cahnge tilte in page.tsx` is historical and left as-is in git.
- The README was later deleted in the branch history, but it was part of the Day 1 setup.
