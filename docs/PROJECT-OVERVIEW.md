# Wazifa.app — Project Overview

The **standalone agent context file** for this project lives at the repository root:

**[`../AI-CONTEXT.md`](/AI-CONTEXT.md)**

Copy or attach that file when working with any AI agent — it includes product context, architecture rules, env vars, mocks/TODOs, health checks, and the full day-by-day curriculum **without requiring other repo files**.

For day-by-day lecture implementation detail (key files, steps, gaps per lecture), see [`index.md`](./index.md) and `docs/day-NN-*/lecture-*.md`.

For compact in-repo agent rules that stay synced with the codebase, see [`../AGENTS.md`](/AGENTS.md).
